## 报错内容
***
<details>
<summary>人才匹配与发展智能辅助Skill开发挑战赛</summary>

#### 你好，`大学生职业方向匹配规划助手` 本次暂不能通过。`SKILL.md:16-23` 会收集专业、技能、兴趣、城市和求职优先级，`SKILL.md:44-59`、`SKILL.md:79-84` 又要求将这些动态字段写入结构化报告和表格，但没有说明入表或进入标题/列表前如何处理 `|`、换行、`< > &`、反引号、链接/图片语法及公式前缀。请补充统一的 Markdown 字段清理规则后重新提交。

</details>

***

<details>
<summary>代码智能辅助与质量保障Skill开发挑战赛</summary>

#### 无

</details>

***

<details>
<summary>全场景内容智能创作Skill开发挑战赛</summary>

#### 你好，`多文献共性观点对比分析` 本次暂不能通过。发布包中 `SKILL.md:56-96` 会把用户提供的文献标题、核心观点和原文句子写入 Markdown 表格、列表和引用块，但没有说明进入输出前如何处理 `|`、换行、HTML 标签、反引号、链接/图片语法、危险协议和 `= + - @` 行首内容。请在输出格式要求中补充动态字段净化规则，并明确原文引用在保留语义的同时需要纯文本化处理后重新提交。

#### 你好，`多文献共性观点对比分析` 本次暂不能通过。发布包中 `SKILL.md:140-149` 的表格字段规则只判断行首是否为 `= + - @`，没有要求先跳过前导空格、制表符、全角空格等空白再判断。` =SUM(1,1)` 或制表符后接 `cmd` 仍可能进入表格。请改为基于首个非空白字符识别这些前缀，并在命中时添加单引号，同时补充示例或自测后重新提交。

</details>

***

<details>
<summary>医疗健康智能理解与辅助决策Skill开发挑战赛</summary>

#### 无

</details>

***

<details>
<summary>多源信息检索与分析Skill开发挑战赛</summary>

#### 你好，`多源学术信息调研综述助手` 本次暂不能通过。`SKILL.md:70-81` 和 `SKILL.md:102-106` 仍要求输出 Markdown 报告、来源汇总表和带 URL 的表格，但没有说明对来源标题、观点摘要和链接展示文本的统一处理规则。请补充表格与链接文本化要求后再提交。

#### 你好，`多源学术信息调研综述助手` 本次暂不能通过。发布包中 `SKILL.md:117`、`SKILL.md:123-125` 要把检索结果标题和 URL 组合成 Markdown 链接，`SKILL.md:135-139` 仅要求处理表格竖线，未说明在来源标题、观点摘要和链接展示文本进入表格或链接前处理 `<` `>` `&`、反引号、`[` `]` `(` `)`、`javascript:`/`data:` 等内容。请补充统一的动态字段清理规则，并要求所有来源标题、摘要、链接展示文本和 URL 先清理或校验后再写入 Markdown 报告。

#### 你好，`多源学术信息调研综述助手` 本次暂不能通过。发布包中 `SKILL.md:145-150` 定义了表格单元格综合清理，覆盖竖线和部分 Markdown 字符，但没有处理单元格中首个非空白字符为 `= + - @` 的情况。来源标题、观点摘要或 URL 展示文本若为 ` =SUM(1,1)` 仍可能进入表格。请补充公式前缀处理规则并加入前导空格/制表符示例后重新提交。

</details>

***

<details>
<summary>政务公文智能处理Skill开发挑战赛</summary>

#### 你好，`政务公文多文档事项对比助手` 本次暂不能通过。发布包中 `SKILL.md:51-95` 会把用户提供的 PDF 文件名、文档标题、查询词、结构化数据和原文段落写入 Markdown 对比表与引用块，但没有要求对这些动态内容做净化；遇到 `|`、换行、HTML 标签、反引号、链接/图片语法、`javascript:`/`data:` 协议或 `= + - @` 行首内容时，报告格式可能被破坏或被解析成非预期内容。请补充统一的 Markdown 表格/正文/引用块处理规则，并确保原文摘录先纯文本化后再输出后重新提交。

#### 你好，`政务公文多文档事项对比助手` 本次暂不能通过。发布包中 `SKILL.md:51-95` 会把用户提供的 PDF 文件名、文档标题、查询词、结构化数据和原文段落写入 Markdown 对比表与引用块，但没有要求对这些动态内容做净化；遇到 `|`、换行、HTML 标签、反引号、链接/图片语法、`javascript:`/`data:` 协议或 `= + - @` 行首内容时，报告格式可能被破坏或被解析成非预期内容。请补充统一的 Markdown 表格/正文/引用块处理规则，并确保原文摘录先纯文本化后再输出后重新提交。

#### 你好，`政务公文多文档事项对比助手` 本次暂不能通过。发布包中 `SKILL.md:48-58` 要把 PDF 原文、标题、单位和查询词写入 Markdown 表格，但表格单元格净化只处理行首 `= + - @`，未说明先跳过前导空格、制表符、全角空格再判断。请按首个非空白字符识别公式前缀并添加单引号或等效纯文本前缀，补充相应用例后重新提交。

</details>

***

<details>
<summary>教育智能辅助与个性化学习Skill开发挑战赛</summary>

#### 你好，`课程教学反馈洞察与改进报告助手 v1.0` 本次暂不能通过。发布包会把学生反馈原文写入 Markdown 表格（`templates/report_template.md:52-66`、`113-116`），但当前清理规则 `SKILL.md:147-152` 和 `templates/report_template.md:5` 只覆盖 `|`、换行和直接以 `= + - @` 开头的内容，未处理 `< > &`、Markdown 链接/图片、`javascript:`/`data:` 协议，也未覆盖前导空格或制表符后的公式前缀。请补充统一字段清理规则：动态字段入表前压平换行、转义管道符与 HTML 特殊字符、去活化 Markdown 链接/图片和危险协议，并按去除前导空白后的首字符检测 `= + - @` 再添加单引号等安全前缀；同步更新模板和自检样例后重新提交。

</details>

***

<details>
<summary>数据智能分析与应用Skill开发挑战赛</summary>

#### 你好，`调研实验数据分析报告助手` 本次暂不能通过。发布包在 `SKILL.md:45-58`、`SKILL.md:98-106` 会把列名、分组名、异常值等用户数据写入 Markdown 表格，但只在 `SKILL.md:192-196` 定义了 CSV/HTML 导出处理，未说明 Markdown 表格字段需要处理 `|`、换行、`< > &`、反引号、链接/图片语法和 `javascript:` 等协议。请补充统一的 Markdown 输出净化规则，确保所有动态字段进入表格、标题和列表前先纯文本化或转义后重新提交。

#### 你好，`调研实验数据分析报告助手` 本次暂不能通过。发布包中 `SKILL.md:236-240` 支持将分析结果导出 CSV，但只要求对以 `= + - @` 开头的单元格加单引号，未说明先处理前导空格、制表符等空白后再判断。用户数据字段如 ` =SUM(1,1)` 或制表符后接 `cmd` 仍可能进入 CSV。请把 CSV 净化规则改为基于首个非空白字符识别公式前缀，并在示例或自检中覆盖该类输入后重新提交。

</details>

***

<details>
<summary>智慧生活助理Skill开发挑战赛</summary>

#### 无

</details>

***

<details>
<summary>智能办公协同助理Skill开发挑战赛</summary>

#### 你好，`会议纪要多源解析报告助手` 本次暂不能通过。发布包中 `SKILL.md:34-36`、`SKILL.md:89-92` 和 `references/report_template.md:103` 会把会议纪要、任务清单、负责人、截止日期、风险项等来自文件的动态内容写入 Markdown/CSV 报告，但当前只说明了公式前缀、HTML 实体和 CSV 特殊字符处理，缺少 Markdown 表格字段清理要求。请补充对 `|`、换行、`< > &`、反引号、`[]()`/`![]()` 链接或图片语法、`javascript:`/`data:` 等异常协议的处理，并要求所有文件解析字段写入报告前先清理后重新提交。

#### 你好，`会议解析状态报告助手 v1.0` 本次暂不能通过。发布包会解析 Excel/CSV、Word 和文本中的任务描述、负责人、截止日期等字段（`SKILL.md:34`、`43`、`46`），并写入多张 Markdown 表格（`references/report_template.md:32`、`55`、`63`、`93`）。虽然 `SKILL.md:90-109` 和 `references/report_template.md:103-122` 已定义字段清理，但 CSV 处理只要求对直接以 `= + - @` 开头的内容添加前缀，Markdown 表格字段也未覆盖公式前缀。请改为按去除前导空白后的首字符检测 `= + - @`，在该有效字符前添加单引号等安全前缀，并补充包含 `  =cmd`、` 	+SUM(...)`、`HYPERLINK(...)` 的样例后重新提交。

</details>

***

<details>
<summary>电商内容智能生成与用户洞察Skill开发挑战赛</summary>

#### SKILL.md:67-93 要求代表性评论原样引用到 Markdown 主题表，SKILL.md:121-144 又要求保持原文，但没有说明如何在保留含义的同时处理竖线、换行、反引号、< > &、Markdown 链接/图片、javascript:/data: 协议和 = + - @ 开头字段。请增加引用前的纯文本化/转义步骤，对评论原句、关键词、主题名等所有动态字段统一处理后再写入表格。

#### 你好，`ecomm-review-insight-miner` 本次暂不能通过。发布包中 `SKILL.md:99-122` 对代表性原句、关键词和主题名的表格清理只检查单元格最开头的 `= + - @`，没有覆盖前导空格、制表符、全角空格后再出现公式前缀的情况。请基于首个非空白字符做判断并添加单引号或等效纯文本前缀，补充 ` =SUM(1,1)`、制表符后接 `cmd` 的自检后重新提交。

</details>

***

<details>
<summary>金融材料智能解析与风险洞察Skill开发挑战赛</summary>

#### 你好，`年报财报智能诊断助手-v2` 本次暂不能通过。发布包中 `SKILL.md:157-159`、`SKILL.md:168-193` 会把公司名称、会计师事务所、关键审计事项、风险描述、原文数据和计算过程写入 Markdown 表格，但没有给出统一的动态字段处理规则。请在写入表格前对这些字段做处理：先去除首尾空白并折叠换行，再转义 `|`、反引号、`<`、`>`、`&`、链接/图片语法；对去除首尾空白后以 `= + - @` 或 `javascript:` 开头的字段前置单引号，避免表格结构被破坏或复制到表格软件后触发公式。

</details>
