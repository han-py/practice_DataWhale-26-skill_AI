## Skill 审核不通过问题归类分析

> 数据范围：2026-07-15 至 2026-07-22，共 2860 条驳回记录，涉及 2578 个独立 Skill。

***

### 一、总览：九大驳回分类及占比

> 注：一条驳回记录可能同时命中多个分类，因此各分类占比之和超过 100%。

|  排名 | 问题分类         |  命中次数 |    占比 | 涉及独立 Skill 数 |
| :-: | ------------ | ----: | ----: | -----------: |
|  1  | 输出内容转义/注入防护  | 2,286 | 79.9% |        2,107 |
|  2  | 外部平台/模型/服务残留 |   516 | 18.0% |          474 |
|  3  | 包结构/命名/元数据   |   438 | 15.3% |          412 |
|  4  | 高风险领域/内容合规边界 |   209 |  7.3% |          189 |
|  5  | 隐私/身份/权限/持久化 |   124 |  4.3% |          118 |
|  6  | 功能正确性/测试/兼容性 |    93 |  3.3% |           83 |
|  7  | 网络访问/SSRF    |    38 |  1.3% |           36 |
|  8  | 其他/需人工复核     |    38 |  1.3% |            — |
|  9  | 危险命令/动态代码执行  |    14 |  0.5% |           14 |

**核心结论：近八成驳回都卡在"输出内容转义/注入防护"上。** 如果直播课只讲一个主题，就讲这个。

***

### 二、各分类详细拆解

#### 第一大类：输出内容转义/注入防护（79.9%，2286 条）

这是压倒性的头号问题。进一步拆分，可以细分为五个子问题：

| 子问题              |  命中次数 | 典型场景                                                                     |
| ---------------- | ----: | ------------------------------------------------------------------------ |
| Markdown 表格字段未转义 | 1,761 | 动态内容（用户输入、检索结果、文件内容）直接写入 Markdown 表格，未处理竖线 `\|`、换行、`< > &`、反引号等          |
| CSV 公式注入未防护      | 1,098 | 写入 CSV 的字段未处理以 `=` `+` `-` `@` 开头的内容，Excel/WPS 打开后可能被当作公式执行              |
| 文件名/路径未净化        |   151 | 用户输入的列名、标题等直接拼入文件名，包含斜杠、`..` 等非法字符时写到非预期路径                               |
| HTML/XSS 未转义     |   170 | 动态内容直接拼入 HTML 的 `<title>`、`<h1>`、`<p>` 等标签，未使用 `html.escape()`           |
| Unicode 编码崩溃     |    48 | 脚本在 Windows GBK 控制台打印 emoji 或特殊符号（如 ⚠️），触发 `UnicodeEncodeError` 导致进程异常退出 |

**直播课建议重点：**

1.  **Markdown 表格转义**（覆盖面最广）：教选手写一个统一的 `sanitize_cell()` 函数，处理竖线、换行、HTML 特殊字符、公式前缀，所有动态字段入表前统一调用。
2.  **CSV 公式注入**：以 `= + - @` 开头的值前置单引号或制表符，防止表格软件执行公式。
3.  **Windows 编码兼容**：脚本入口统一设置 `sys.stdout.reconfigure(encoding='utf-8')`，或用 ASCII 替代 emoji 输出。

***

#### 第二大类：外部平台/模型/服务残留（18.0%，516 条）

| 常见残留类型    | 典型表现                                                    |
| --------- | ------------------------------------------------------- |
| 境外大模型引用   | 代码/注释/函数名中残留 `openai`、`claude`、`gemini`、`anthropic` 等命名 |
| 配置文件命名    | `agents/openai.yaml` 等文件名含境外服务名称                        |
| 境外平台链接    | 列出 ClawHub 全球、SkillsMP、Agent Skills 等境外 Skill 平台入口      |
| 境外资源引用    | Google Fonts、Google Trends、YouTube、TikTok 等平台链接或引导      |
| 境外 API 引导 | 文档引导用户去 OpenAI 等平台注册获取 API Key                          |
| 外部 CDN 依赖 | 交付 HTML 中加载 `cdn.jsdelivr.net` 等外部脚本                    |
| 特定平台入口    | `CLAUDE.md`、`ARCHITECTURE.md` 等平台特定文件引用                 |

**直播课建议重点：** 提交前做一次全量关键词搜索（`openai`、`claude`、`gemini`、`google`、`youtube`、`CLAUDE.md` 等），确保代码、注释、文件名、文档、示例中无残留。GitHub 等技术社区的正常引用可以保留，但需要确认是技术上下文而非引导访问。

***

#### 第三大类：包结构/命名/元数据（15.3%，438 条）

这类问题通常与第一、第二类同时出现（组合出现 222 条），主要表现为：

*   SKILL.md 中引用了不存在的文件或路径
*   示例命令无法按文档说明复现
*   文件命名含特殊字符或不规范结构
*   元数据（名称、版本、描述）不符合规范

**直播课建议重点：** 提交前用文档中的示例命令实际跑一遍，确认能 0 退出码完成；检查所有引用路径是否真实存在。

***

#### 第四大类：高风险领域/内容合规边界（7.3%，209 条）

| 高风险场景     | 典型问题                                                      |
| --------- | --------------------------------------------------------- |
| 医疗/用药建议   | 给出具体药物名称、剂量、用法（如"铁剂、钙片"、"避免联用"、"按公斤体重减量"），而非"请咨询医生"的原则性提醒 |
| 政治/意识形态内容 | 包含政治术语检查、宣传稿代写、理论学习心得生成等定位                                |
| 政府公文辅助    | 引导生成特定政治表述、内置政治术语库                                        |
| 校园政策宣传    | 面向党员之家、思政辅导员等角色的宣传稿生成                                     |

**直播课建议重点：** 医疗类 Skill 只给"建议咨询医生"的提醒，不给具体用药方案；涉及政治、政府公文场景的 Skill 需调整为中性定位，移除特定角色和宣传性内容。

***

#### 第五大类：隐私/身份/权限/持久化（4.3%，124 条）

*   未对姓名、手机号、邮箱、身份证号、工号等个人信息做脱敏
*   评论原文、简历内容等直接写入输出文件
*   缺少隐私声明和免责声明

**直播课建议重点：** 在输出模板中默认对个人 identifiable information（PII）做掩码处理（如 `138****1234`），并在文档中说明数据处理方式。

***

#### 第六大类：功能正确性/测试/兼容性（3.3%，93 条）

| 子问题          | 典型表现                                                   |
| ------------ | ------------------------------------------------------ |
| 单元测试失败       | `python -m unittest discover` 跑不通，golden file 与实际输出不一致 |
| 样例命令报错       | 按 SKILL.md 中的示例命令运行，程序非 0 退出码                          |
| 列数/字段不对齐     | 表头定义和实际写入的列数不一致                                        |
| Windows 编码崩溃 | 与第一大类重叠，GBK 控制台打印 emoji 导致 `UnicodeEncodeError`        |

**直播课建议重点：** 提交前务必跑一遍包内自带的测试和示例命令，确保在 Windows 默认控制台环境下也能正常运行。

***

#### 第七大类：网络访问/SSRF（1.3%，38 条）

*   用户可传入任意 URL，脚本未做内网地址过滤
*   未限制只允许 `https` 协议
*   未拦截 `localhost`、`127.0.0.1`、`169.254.x.x` 等内网/链路本地地址
*   未做 DNS 解析后的二次 IP 校验

**直播课建议重点：** 如果 Skill 需要接受用户 URL 输入，必须做协议白名单（只允许 https）、域名/IP 黑名单（拦截内网地址）、DNS 解析后二次校验。或者改用平台受控的检索/抓取能力。

***

#### 第八大类：危险命令/动态代码执行（0.5%，14 条）

*   使用 `exec()`/`eval()` 执行用户提交的代码
*   `os.system()` 拼接用户输入
*   发布包中包含可直接运行的不安全示例代码（硬编码密钥、SQL 拼接、pickle.loads）
*   Shell 脚本中未对用户输入做 `shlex.quote()` 转义

**直播课建议重点：** 示例代码改为不可执行的文档片段或伪代码；必须执行动态代码时使用沙箱隔离。

***

### 三、重复提交分析

|   被拒次数 | Skill 数量 | 说明              |
| -----: | -------: | --------------- |
|    1 次 |    2,360 | 绝大多数选手只被拒一次     |
|    2 次 |      178 | 修改后重新提交仍未完全修复   |
|    3 次 |       27 | 反复修改但遗漏了关联问题    |
| 4 次及以上 |       13 | 需要重点关注，可能存在理解偏差 |

被拒最多的 Skill：`content-creator-skill`（9 次）、`insightflow-skill`（6 次）、`medical-report-reader`（6 次）、`老人健康多源解析照护助手`（6 次）。

**直播课建议：** 强调"修改后重新提交前要做全量复查"，不能只修被指出的那一个点，同系列的其他文件可能也有同类问题。

***

### 四、直播课建议大纲

根据以上分析，建议直播课按以下优先级安排内容：

**第一部分（必讲，覆盖 80% 驳回）——输出安全处理**

*   写一个通用的 `sanitize_cell()` 函数：处理 Markdown 竖线、换行、HTML 特殊字符、CSV 公式前缀
*   Windows 编码兼容：`sys.stdout.reconfigure(encoding='utf-8')`，避免 emoji 输出
*   文件名净化：白名单化用户输入，校验最终路径在输出目录内

**第二部分（必讲，覆盖 18% 驳回）——外部平台清理**

*   全量搜索关键词清单：`openai`、`claude`、`gemini`、`anthropic`、`google`、`youtube`、`CLAUDE.md` 等
*   区分"技术引用"和"引导访问"的边界
*   配置文件和函数命名中的残留清理

**第三部分（建议讲）——包结构与元数据规范**

*   提交前自测：用文档中的示例命令实际跑一遍
*   检查所有引用路径是否真实存在
*   示例输出与示例数据一致性校验

**第四部分（选讲）——高风险场景合规**

*   医疗类：只给"请咨询医生"提醒，不给具体用药方案
*   政治/政务类：调整为中性定位，移除特定角色和宣传性内容
*   隐私保护：PII 默认脱敏，文档中声明数据处理方式

**第五部分（选讲）——代码安全基础**

*   避免 `exec()`/`eval()`/`os.system()` 拼接用户输入
*   URL 输入的 SSRF 防护
*   示例代码中不留可执行的不安全代码

***

### 五、一句话总结

> **80% 的驳回都是因为"动态内容未做转义就直接输出"。** 教会选手写一个统一的输出清洗函数，并在提交前全量搜索清理外部平台引用，就能解决绝大部分问题。

